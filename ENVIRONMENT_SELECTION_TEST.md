# تست انتخاب محیط‌ها و سنسورها

## 🎯 هدف
تست عملکرد کادر سبز رنگ دور آیتم‌های انتخاب شده در لیست محیط‌ها و سنسورها

## 🧪 مراحل تست

### 1. اضافه کردن محیط‌ها و سنسورها
1. پروژه را اجرا کنید: `npm run dev`
2. در پنل Scene، بخش Environment را باز کنید
3. از کاتالوگ محیط‌ها، چندین محیط اضافه کنید:
   - Room 1
   - Room 2
   - Room 3
4. از کاتالوگ سنسورها، چندین سنسور اضافه کنید:
   - Temperature
   - Humidity
   - CO2

### 2. تست انتخاب محیط‌ها
1. روی یکی از محیط‌ها در لیست کلیک کنید
2. **انتظار:** کادر سبز رنگ دور آیتم ظاهر شود
3. روی محیط دیگری کلیک کنید
4. **انتظار:** کادر سبز از آیتم قبلی حذف شود و به آیتم جدید منتقل شود

### 3. تست انتخاب سنسورها
1. روی یکی از سنسورها در لیست کلیک کنید
2. **انتظار:** کادر سبز رنگ دور آیتم ظاهر شود
3. روی سنسور دیگری کلیک کنید
4. **انتظار:** کادر سبز از آیتم قبلی حذف شود و به آیتم جدید منتقل شود

### 4. تست تغییر انتخاب بین محیط و سنسور
1. یک محیط را انتخاب کنید
2. **انتظار:** کادر سبز دور محیط ظاهر شود
3. یک سنسور را انتخاب کنید
4. **انتظار:** کادر سبز از محیط حذف شود و دور سنسور ظاهر شود

### 5. تست انتخاب از صحنه 3D
1. یک محیط را از لیست انتخاب کنید
2. **انتظار:** کادر سبز دور محیط ظاهر شود
3. روی یک سنسور در صحنه 3D کلیک کنید
4. **انتظار:** کادر سبز از محیط حذف شود و دور سنسور در لیست ظاهر شود
5. روی یک محیط در صحنه 3D کلیک کنید
6. **انتظار:** کادر سبز از سنسور حذف شود و دور محیط در لیست ظاهر شود

### 6. تست لغو انتخاب
1. روی فضای خالی در صحنه کلیک کنید
2. **انتظار:** کادر سبز از همه آیتم‌ها حذف شود
3. یا کلید Escape را فشار دهید
4. **انتظار:** کادر سبز از همه آیتم‌ها حذف شود

### 7. تست Hover Effect
1. ماوس را روی آیتم انتخاب شده ببرید
2. **انتظار:** کادر سبز تیره‌تر شود و glow effect قوی‌تر شود

## ✅ نتایج مورد انتظار

### انتخاب محیط یا سنسور:
- کادر سبز 2px با رنگ `#22c55e`
- پس‌زمینه سبز روشن `rgba(34,197,94,.15)`
- سایه سبز `0 0 8px rgba(34,197,94,.3)`

### Hover روی آیتم انتخاب شده:
- کادر سبز تیره‌تر `#16a34a`
- پس‌زمینه سبز تیره‌تر `rgba(34,197,94,.25)`
- سایه سبز قوی‌تر `0 0 12px rgba(34,197,94,.4)`

### لغو انتخاب:
- کادر سبز حذف شود
- پس‌زمینه به حالت عادی برگردد
- سایه حذف شود

## 🐛 عیب‌یابی

### اگر کادر سبز ظاهر نمی‌شود:
1. Console را بررسی کنید برای خطاهای JavaScript
2. مطمئن شوید که `updateEnvironmentList()` فراخوانی می‌شود
3. CSS classes `selected` به درستی اعمال می‌شوند

### اگر کادر سبز حذف نمی‌شود:
1. مطمئن شوید که `clearSelection()` فراخوانی می‌شود
2. `selectedId` به `null` تنظیم می‌شود
3. `updateEnvironmentList()` بعد از لغو انتخاب فراخوانی می‌شود

## 📝 یادداشت‌ها
- انتخاب محیط‌ها و سنسورها با کلیک روی آیتم در لیست یا صحنه 3D انجام می‌شود
- لغو انتخاب با کلیک روی فضای خالی یا فشار Escape انجام می‌شود
- کادر سبز فقط برای آیتم‌های انتخاب شده نمایش داده می‌شود
- Transition smooth برای تغییرات visual وجود دارد
- انتخاب بین محیط و سنسور متقابل است (فقط یکی می‌تواند انتخاب شود)
- انتخاب از هر منبع (لیست یا صحنه 3D) باعث لغو انتخاب قبلی می‌شود
- هر دو لیست (محیط‌ها و سنسورها) همزمان به‌روزرسانی می‌شوند

---
**تاریخ تست:** دسامبر 2024  
**وضعیت:** ✅ آماده تست

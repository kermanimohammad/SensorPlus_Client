# بهبودهای سیستم انتخاب و ترنسفورم

## تغییرات اعمال شده

### 1. فعال‌سازی انتخاب مدل‌های محیط
- **مشکل**: مدل‌های محیط `isPickable = false` بودند
- **راه‌حل**: اضافه کردن `mesh.isPickable = true` برای تمام mesh های محیط در `src/env.ts`

### 2. بهبود سیستم انتخاب
- **مشکل**: انتخاب محیط‌ها به درستی کار نمی‌کرد
- **راه‌حل**: 
  - استفاده از شناسه منحصر به فرد `env_${envId}` برای محیط‌ها
  - بهبود تابع `attachToCurrentSelection()` برای تشخیص نوع انتخاب
  - اضافه کردن پشتیبانی کامل از ترنسفورم محیط‌ها

### 3. بازخورد بصری
- **مشکل**: مدل‌های انتخاب شده بازخورد بصری نداشتند
- **راه‌حل**:
  - اضافه کردن تابع `addSelectionHighlight()` برای outline زرد
  - اضافه کردن تابع `removeSelectionHighlight()` برای حذف highlight
  - اعمال highlight به سنسورها و محیط‌های انتخاب شده

### 4. بهبود تجربه کاربری
- **مشکل**: کلیک روی فضای خالی انتخاب را لغو نمی‌کرد
- **راه‌حل**: اضافه کردن تشخیص کلیک روی فضای خالی برای لغو انتخاب

### 5. بهبود سیستم حذف
- **مشکل**: حذف محیط‌ها و سنسورها به درستی کار نمی‌کرد
- **راه‌حل**: بهبود تابع حذف برای تشخیص نوع شیء و حذف مناسب

## نحوه کارکرد جدید

### انتخاب سنسور
1. کلیک روی سنسور → انتخاب سنسور
2. نمایش highlight زرد
3. نمایش popup اطلاعات
4. فعال‌سازی ابزار ترنسفورم در صورت انتخاب

### انتخاب محیط
1. کلیک روی مدل محیط → انتخاب محیط
2. نمایش highlight زرد
3. مخفی کردن popup سنسور
4. فعال‌سازی ابزار ترنسفورم در صورت انتخاب

### لغو انتخاب
1. کلیک روی فضای خالی → لغو انتخاب
2. حذف highlight
3. مخفی کردن popup
4. بازگشت به حالت انتخاب

### ترنسفورم
- **Move Tool**: جابجایی سنسور یا محیط
- **Rotate Tool**: چرخش سنسور یا محیط  
- **Scale Tool**: تغییر اندازه سنسور یا محیط
- **Delete Tool**: حذف سنسور یا محیط انتخاب شده

## تست عملکرد

برای تست بهبودها:

1. **تست انتخاب سنسور**:
   - سنسور اضافه کنید
   - روی آن کلیک کنید
   - باید highlight زرد نمایش داده شود
   - ابزار ترنسفورم باید فعال شود

2. **تست انتخاب محیط**:
   - فایل GLB آپلود کنید
   - روی مدل کلیک کنید
   - باید highlight زرد نمایش داده شود
   - ابزار ترنسفورم باید فعال شود

3. **تست ترنسفورم**:
   - ابزار Move/Rotate/Scale را انتخاب کنید
   - روی مدل کلیک کنید
   - باید gizmo نمایش داده شود
   - تغییرات باید اعمال شود

4. **تست لغو انتخاب**:
   - مدلی را انتخاب کنید
   - روی فضای خالی کلیک کنید
   - انتخاب باید لغو شود
   - highlight باید حذف شود

## نکات مهم

- هر بار فقط یک مدل قابل انتخاب است
- انتخاب جدید، انتخاب قبلی را لغو می‌کند
- مدل‌های محیط و سنسورها هر دو قابل انتخاب و ترنسفورم هستند
- تغییرات در real-time ذخیره می‌شوند
- سیستم highlight بصری برای تشخیص انتخاب فعال است

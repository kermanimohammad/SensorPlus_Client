# 🧪 راهنمای تست ورژن نامبر

## 🎯 هدف
تست نمایش ورژن نامبر در گوشه پایین راست صفحه.

## 📋 مراحل تست

### **مرحله 1: باز کردن پروژه**
1. پروژه را در مرورگر باز کنید
2. صفحه باید کاملاً لود شود

### **مرحله 2: بررسی نمایش ورژن**
1. به گوشه **پایین راست** صفحه نگاه کنید
2. باید ورژن نامبر **"v1.0.0"** نمایش داده شود
3. ورژن نامبر باید با استایل زیر ظاهر شود:
   - پس‌زمینه شفاف مشکی
   - متن سفید
   - فونت Courier New
   - گوشه‌های گرد
   - سایه و border

### **مرحله 3: تست Tooltip**
1. ماوس را روی ورژن نامبر قرار دهید
2. Tooltip باید نمایش داده شود با اطلاعات:
   - **Build:** 2024.12.19
   - **Features:** لیست کامل ویژگی‌ها

### **مرحله 4: تست Responsive**
1. اندازه پنجره مرورگر را تغییر دهید
2. ورژن نامبر باید در گوشه پایین راست باقی بماند
3. موقعیت باید ثابت باشد

### **مرحله 5: تست تعامل**
1. روی ورژن نامبر کلیک کنید
   - **انتظار:** هیچ اتفاقی نیفتد (غیرقابل کلیک)
2. سعی کنید ورژن نامبر را انتخاب کنید
   - **انتظار:** غیرقابل انتخاب باشد

## 🔍 بررسی‌های فنی

### **Console Logs:**
1. Console را باز کنید (F12)
2. باید پیام زیر را ببینید (یا نباشد):
   ```
   Could not load version.json: [error]
   ```
3. اگر خطا وجود دارد، فایل `version.json` در مسیر صحیح نیست

### **Network Tab:**
1. Network tab را در Developer Tools باز کنید
2. صفحه را refresh کنید
3. باید درخواست `version.json` را ببینید
4. Status باید 200 باشد

### **Elements Tab:**
1. Elements tab را باز کنید
2. عنصر `#versionNumber` را پیدا کنید
3. باید محتوای زیر را داشته باشد:
   ```html
   <div id="versionNumber" class="version-number">
     v1.0.0
   </div>
   ```

## 🎨 بررسی استایل

### **CSS Properties:**
```css
.version-number {
  position: fixed;
  bottom: 10px;
  right: 10px;
  background: rgba(0, 0, 0, 0.7);
  color: #ffffff;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  font-weight: bold;
  z-index: 1000;
  pointer-events: none;
  user-select: none;
  backdrop-filter: blur(4px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
}
```

### **Visual Checks:**
- ✅ موقعیت: گوشه پایین راست
- ✅ پس‌زمینه: شفاف مشکی
- ✅ متن: سفید و bold
- ✅ فونت: Courier New
- ✅ سایه: موجود
- ✅ Border: موجود
- ✅ Backdrop blur: موجود

## 🚨 مشکلات احتمالی

### **مشکل 1: ورژن نامبر نمایش داده نمی‌شود**
**علت:** فایل `version.json` لود نمی‌شود
**راه‌حل:**
1. بررسی کنید که `version.json` در root directory موجود است
2. Console را برای خطاهای JavaScript بررسی کنید
3. Network tab را برای درخواست‌های ناموفق بررسی کنید

### **مشکل 2: استایل درست نیست**
**علت:** CSS لود نمی‌شود
**راه‌حل:**
1. بررسی کنید که `styles.css` لود شده است
2. Elements tab را بررسی کنید
3. CSS properties را در Developer Tools بررسی کنید

### **مشکل 3: Tooltip نمایش داده نمی‌شود**
**علت:** JavaScript لود نمی‌شود
**راه‌حل:**
1. Console را برای خطاهای JavaScript بررسی کنید
2. بررسی کنید که `loadVersion()` فراخوانی می‌شود
3. بررسی کنید که `version.json` درست parse می‌شود

## 📊 نتایج مورد انتظار

### **✅ تست موفق:**
- ورژن نامبر در گوشه پایین راست نمایش داده می‌شود
- استایل درست است (شفاف، سفید، سایه)
- Tooltip با اطلاعات کامل نمایش داده می‌شود
- موقعیت ثابت است
- غیرقابل کلیک و انتخاب است

### **❌ تست ناموفق:**
- ورژن نامبر نمایش داده نمی‌شود
- استایل اشتباه است
- Tooltip کار نمی‌کند
- موقعیت ثابت نیست
- قابل کلیک یا انتخاب است

## 🔄 تست تغییر ورژن

### **مرحله 1: تغییر ورژن**
1. فایل `version.json` را باز کنید
2. `version` را به `1.0.1` تغییر دهید
3. `build` را به تاریخ فعلی تغییر دهید

### **مرحله 2: Build و تست**
1. `npm run build` اجرا کنید
2. پروژه را refresh کنید
3. ورژن نامبر باید `v1.0.1` نمایش دهد

---
**تاریخ ایجاد:** دسامبر 2024  
**وضعیت:** 🧪 آماده تست

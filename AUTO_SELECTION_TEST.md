# 🎯 تست انتخاب خودکار مدل‌های جدید

## 📋 هدف
تست عملکرد انتخاب خودکار مدل‌های جدید (محیط و سنسور) هنگام ایجاد در صحنه.

## 🧪 سناریوهای تست

### 1️⃣ **تست انتخاب خودکار محیط جدید**

#### **مرحله 1: ایجاد محیط از کاتالوگ**
1. پروژه را باز کنید
2. در پنل Scene، بخش Environment Catalog را پیدا کنید
3. یک محیط از کاتالوگ انتخاب کنید (مثلاً Room 1)
4. روی دکمه "Add" کلیک کنید

#### **نتیجه مورد انتظار:**
- ✅ محیط جدید در صحنه ظاهر می‌شود
- ✅ کادر سبز دور محیط جدید در لیست Environment ظاهر می‌شود
- ✅ کادر سبز از همه سنسورها حذف می‌شود
- ✅ محیط جدید به عنوان active environment تنظیم می‌شود

#### **مرحله 2: ایجاد محیط از فایل**
1. روی دکمه "Load Environment" کلیک کنید
2. یک فایل GLB انتخاب کنید
3. فایل را بارگذاری کنید

#### **نتیجه مورد انتظار:**
- ✅ محیط جدید در صحنه ظاهر می‌شود
- ✅ کادر سبز دور محیط جدید در لیست Environment ظاهر می‌شود
- ✅ کادر سبز از همه سنسورها حذف می‌شود
- ✅ محیط جدید به عنوان active environment تنظیم می‌شود

### 2️⃣ **تست انتخاب خودکار سنسور جدید**

#### **مرحله 1: ایجاد سنسور جدید**
1. در پنل Scene، بخش Sensor را پیدا کنید
2. یک نوع سنسور انتخاب کنید (مثلاً Temperature)
3. روی دکمه "Add Sensor" کلیک کنید

#### **نتیجه مورد انتظار:**
- ✅ سنسور جدید در صحنه ظاهر می‌شود
- ✅ کادر سبز دور سنسور جدید در لیست Sensors ظاهر می‌شود
- ✅ کادر سبز از همه محیط‌ها حذف می‌شود
- ✅ سنسور جدید انتخاب می‌شود
- ✅ پنل Scene Properties نمایش داده می‌شود
- ✅ Popup سنسور نمایش داده می‌شود

### 3️⃣ **تست تغییر انتخاب متقابل**

#### **مرحله 1: ایجاد محیط و سنسور**
1. یک محیط از کاتالوگ اضافه کنید
2. یک سنسور اضافه کنید

#### **مرحله 2: تغییر انتخاب**
1. روی محیط در لیست Environment کلیک کنید
2. روی سنسور در لیست Sensors کلیک کنید
3. روی محیط در صحنه 3D کلیک کنید
4. روی سنسور در صحنه 3D کلیک کنید

#### **نتیجه مورد انتظار:**
- ✅ هر بار که آیتم جدیدی انتخاب می‌شود، کادر سبز از آیتم قبلی حذف می‌شود
- ✅ کادر سبز فقط دور آیتم انتخاب شده ظاهر می‌شود
- ✅ هر دو لیست (Environment و Sensors) همزمان به‌روزرسانی می‌شوند

### 4️⃣ **تست لود پروژه**

#### **مرحله 1: ذخیره و لود پروژه**
1. یک پروژه با محیط و سنسور ایجاد کنید
2. پروژه را ذخیره کنید
3. پروژه را لود کنید

#### **نتیجه مورد انتظار:**
- ✅ همه محیط‌ها و سنسورها لود می‌شوند
- ✅ هیچ آیتمی به صورت خودکار انتخاب نمی‌شود
- ✅ `selectedId` برابر `null` است
- ✅ کادر سبز دور هیچ آیتمی ظاهر نمی‌شود

## 🔍 بررسی کد

### **توابع اصلاح شده:**

#### **1. `addEnvironmentFromGLBArrayBuffer` (env.ts)**
```typescript
setActiveEnvironment(envId);

// انتخاب محیط جدید و لغو انتخاب قبلی
(window as any).selectedId = `env_${envId}`;
updateEnvironmentList();
updateSensorList();
return envId;
```

#### **2. `addEnvironmentFromGLBFile` (env.ts)**
```typescript
setActiveEnvironment(envId);

// انتخاب محیط جدید و لغو انتخاب قبلی
(window as any).selectedId = `env_${envId}`;
updateEnvironmentList();
updateSensorList();
return envId;
```

#### **3. `addEnvironmentFromProjectB64` (env.ts)**
```typescript
if (makeActive) {
  activeEnvId = id;
  // انتخاب محیط جدید و لغو انتخاب قبلی
  (window as any).selectedId = `env_${id}`;
  updateEnvironmentList();
  updateSensorList();
} else {
  updateEnvironmentList();
}
```

#### **4. ایجاد سنسور جدید (main.ts)**
```typescript
// انتخاب سنسور جدید
(window as any).selectedId = id;

// به‌روزرسانی لیست سنسورها و لغو انتخاب محیط‌ها
updateSensorList();
updateEnvironmentList();
```

## 🚨 مشکلات احتمالی

### **اگر محیط جدید انتخاب نمی‌شود:**
1. مطمئن شوید که `selectedId` به درستی تنظیم می‌شود
2. `updateEnvironmentList()` و `updateSensorList()` فراخوانی می‌شوند
3. CSS class `selected` به درستی اعمال می‌شود

### **اگر کادر سبز حذف نمی‌شود:**
1. مطمئن شوید که هر دو تابع `updateEnvironmentList()` و `updateSensorList()` فراخوانی می‌شوند
2. `selectedId` به درستی تغییر می‌کند
3. CSS transition به درستی کار می‌کند

### **اگر لود پروژه انتخاب خودکار دارد:**
1. مطمئن شوید که `recreateSensorFromSerialized` انتخاب خودکار ندارد
2. `selectedId` در انتهای لود به `null` تنظیم می‌شود

## 📝 یادداشت‌ها
- انتخاب خودکار فقط برای مدل‌های جدید ایجاد شده توسط کاربر اعمال می‌شود
- لود پروژه نباید انتخاب خودکار داشته باشد
- هر دو لیست (Environment و Sensors) همزمان به‌روزرسانی می‌شوند
- انتخاب متقابل بین محیط و سنسور حفظ می‌شود

---
**تاریخ تست:** دسامبر 2024  
**وضعیت:** ✅ آماده تست
